# Text-guided-image-retrieval-system
Text-guided image retrieval system using CLIP (Contrastive Language-Image Pretraining) to find semantically relevant images from text queries. The project encodes text and images into a shared embedding space, ranks results by similarity, and evaluates performance using Precision, Recall, F1 Score, and Hit@1.
